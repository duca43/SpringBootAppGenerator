package ${packageBase}.repository;

import ${packageBase}.repository.base.${entity.name}RepositoryBase;
import org.springframework.stereotype.Repository;

@Repository
public interface ${entity.name}Repository extends ${entity.name}RepositoryBase {
}